[Unit]
Description=Mount fw32 root directories at startup.
After=syslog.target

[Service]
ExecStart=/usr/bin/fw32-mount-all
ExecStop=/usr/bin/fw32-umount-all
Type=oneshot
StandardOutput=syslog
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
